# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Node / React ===
node_modules/
dist/
build/
.next/
*.local

# === Terraform ===
# state contains resource IDs / secrets — never commit
*.tfstate
*.tfstate.*
# 変数ファイルは DSN 等の秘密を持つのでコミットしない
*.tfvars
*.tfvars.json
!example.tfvars
.terraform/
# .terraform.lock.hcl IS committed on purpose (pins provider versions)

# === Secrets / env ===
.env
.env.*
!.env.example
*.pem
credentials.csv

# === OS / editor ===
.DS_Store
.idea/
.vscode/

# === Lambda build artifacts ===
infra/build/
